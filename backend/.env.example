# Backend Environment Configuration
# Copy this file to .env and update the values for your environment

# ============== MongoDB Configuration ==============
# MongoDB connection URL
# Local: mongodb://localhost:27017
# Docker: mongodb://mongodb:27017 (if using docker-compose)
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URL=mongodb://localhost:27017

# Database name
DATABASE_NAME=bazaar_baba


# ============== Server Configuration ==============
# Host to bind the server to
# 0.0.0.0 = all interfaces (needed for Docker)
# 127.0.0.1 = localhost only
HOST=0.0.0.0

# Port to run the server on
PORT=8000


# ============== CORS Configuration ==============
# Comma-separated list of allowed origins
# Development: Include all local dev servers
# Production: Only your production domain(s)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5500,http://127.0.0.1:5500,http://127.0.0.1:3000


# ============== Production Settings ==============
# Uncomment and configure for production deployment

# MONGODB_URL=mongodb+srv://prod_user:password@cluster.mongodb.net/
# DATABASE_NAME=bazaar_baba_prod
# ALLOWED_ORIGINS=https://bazaarbaba.com,https://www.bazaarbaba.com
# HOST=0.0.0.0
# PORT=8000


# ============== Optional Settings ==============
# Add any additional environment variables your app needs

# LOG_LEVEL=INFO
# ENABLE_SWAGGER_UI=true
